# DevOps Nginx demo

## Introduction

This project helps you install nginx on linux and then show a demo page

## steps
sudo apt-get update
sudo apt install nginx -y
sudo systemctl start nginx
sudo systemctl enable nginx

### This is Nginx project
 
